<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    ============================================================
    Project    : 관제 티켓 (Ticketing System)
    FileName   : Goods_SQL.xml
    Namespace  : concert
    Developer  : 주재현 (feature/jjh)
    Created    : 2026.05.22
    Modified   : 2026.05.26
    Description: 공연 매퍼
                 - CLOB → String 자동 변환
                 - 대소문자 무관 검색 (UPPER)
                 - 스케줄: bookingOpenable / performanceEnded 플래그 제공
    ============================================================
-->
<mapper namespace="concert">

    <resultMap id="concertMap" type="java.util.HashMap">
        <id     property="concertId"   column="CONCERT_ID"  jdbcType="NUMERIC"/>
        <result property="title"       column="TITLE"       jdbcType="VARCHAR"/>
        <result property="artist"      column="ARTIST"      jdbcType="VARCHAR"/>
        <result property="venue"       column="VENUE"       jdbcType="VARCHAR"/>
        <result property="description" column="DESCRIPTION" jdbcType="CLOB" javaType="java.lang.String"/>
        <result property="thumbnail"   column="THUMBNAIL"   jdbcType="VARCHAR"/>
        <result property="status"      column="STATUS"      jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 스케줄 resultMap - 플래그 컬럼 포함 -->
    <resultMap id="scheduleMap" type="java.util.HashMap">
        <id     property="scheduleId"        column="SCHEDULE_ID"        jdbcType="NUMERIC"/>
        <result property="concertId"         column="CONCERT_ID"         jdbcType="NUMERIC"/>
        <result property="performanceDate"   column="PERFORMANCE_DATE"   jdbcType="VARCHAR"/>
        <result property="bookingOpenAt"     column="BOOKING_OPEN_AT"    jdbcType="VARCHAR"/>
        <result property="totalSeats"        column="TOTAL_SEATS"        jdbcType="NUMERIC"/>
        <result property="availableSeats"    column="AVAILABLE_SEATS"    jdbcType="NUMERIC"/>
        <result property="weekday"           column="WEEKDAY"            jdbcType="VARCHAR"/>
        <result property="performanceTime"   column="PERFORMANCE_TIME"   jdbcType="VARCHAR"/>
        <result property="bookingOpenable"   column="BOOKING_OPENABLE"   jdbcType="NUMERIC"/>
        <result property="performanceEnded"  column="PERFORMANCE_ENDED"  jdbcType="NUMERIC"/>
    </resultMap>


    <!-- ============================================================ -->
    <!-- 공연 목록                                                    -->
    <!-- ============================================================ -->
    <select id="selectConcertList" parameterType="map" resultMap="concertMap">
        SELECT concert_id, title, artist, venue, description, thumbnail, status
        FROM concerts
        <where>
            <if test="keyword != null and keyword != ''">
                AND (UPPER(title)  LIKE '%' || UPPER(#{keyword}) || '%'
                  OR UPPER(artist) LIKE '%' || UPPER(#{keyword}) || '%'
                  OR UPPER(venue)  LIKE '%' || UPPER(#{keyword}) || '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        <choose>
            <when test='orderBy == "title"'>  ORDER BY title  ASC, concert_id DESC</when>
            <when test='orderBy == "artist"'> ORDER BY artist ASC, concert_id DESC</when>
            <otherwise>                       ORDER BY concert_id DESC</otherwise>
        </choose>
    </select>

    <!-- ============================================================ -->
    <!-- 공연 상세                                                    -->
    <!-- ============================================================ -->
    <select id="selectConcertDetail" parameterType="long" resultMap="concertMap">
        SELECT concert_id, title, artist, venue, description, thumbnail, status
        FROM concerts
        WHERE concert_id = #{concertId}
    </select>

    <!-- ============================================================ -->
    <!-- 통합 검색                                                    -->
    <!-- ============================================================ -->
    <select id="searchConcerts" parameterType="string" resultMap="concertMap">
        SELECT concert_id, title, artist, venue, description, thumbnail, status
        FROM concerts
        <where>
            <if test="_parameter != null and _parameter != ''">
                  UPPER(title)  LIKE '%' || UPPER(#{keyword}) || '%'
               OR UPPER(artist) LIKE '%' || UPPER(#{keyword}) || '%'
               OR UPPER(venue)  LIKE '%' || UPPER(#{keyword}) || '%'
            </if>
        </where>
        ORDER BY concert_id DESC
    </select>

    <!-- ============================================================ -->
    <!-- 공연 스케줄 목록                                              -->
    <!--                                                              -->
    <!-- 플래그 컬럼:                                                  -->
    <!--   booking_openable  : 예매 오픈 가능?     (1=가능 0=대기)     -->
    <!--   performance_ended : 공연 일자 지났음?   (1=종료 0=미래)     -->
    <!--                                                              -->
    <!-- 정렬:                                                         -->
    <!--   1) 미래 일정 먼저 (performance_ended ASC)                  -->
    <!--   2) 공연일자 빠른 순 (performance_date ASC)                 -->
    <!-- ============================================================ -->
    <select id="selectScheduleListByConcertId" parameterType="long" resultMap="scheduleMap">
        SELECT
              schedule_id
            , concert_id
            , TO_CHAR(performance_date, 'YYYY-MM-DD HH24:MI') AS performance_date
            , TO_CHAR(booking_open_at,  'YYYY-MM-DD HH24:MI') AS booking_open_at
            , total_seats
            , available_seats
            , TO_CHAR(performance_date, 'DY', 'NLS_DATE_LANGUAGE=KOREAN') AS weekday
            , TO_CHAR(performance_date, 'HH24:MI') AS performance_time
            , CASE WHEN booking_open_at  &lt;= SYSTIMESTAMP THEN 1 ELSE 0 END AS booking_openable
            , CASE WHEN performance_date &lt;  SYSTIMESTAMP THEN 1 ELSE 0 END AS performance_ended
        FROM concert_schedules
        WHERE concert_id = #{concertId}
        ORDER BY
              CASE WHEN performance_date &lt; SYSTIMESTAMP THEN 1 ELSE 0 END ASC
            , performance_date ASC
    </select>

</mapper>
