<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
============================================================
 Project   : 관제 티켓 (Ticketing System)
 FileName  : Admin_SQL.xml
 Namespace : admin

 Modified  : 2026.05.26

 Description :
   - 관리자 화면 SQL (MyBatis)
   - 실제 테이블: members / concerts / concert_schedules / bookings

 History :
   2026.05.25 - 탬퍼 감지 SQL 제거 (보안 모니터링은 Splunk 측에서 처리)
                payments 테이블 JOIN 제거
   2026.05.25 - 검색 대소문자 무시 처리 (LOWER 함수 적용)
                · concerts(title/artist), members(email/name),
                  bookings(email/name/concert) 모두 적용
   2026.05.25 - 스마트 공연 삭제용 SQL 추가
                · countBookingsByConcert / deleteConcertHard 등 7개
   2026.05.26 - 회원 권한 관리 SQL 추가
                · selectMember         : 회원 단건 조회 (보호 장치 검증용)
                · countAdmin           : ADMIN role 카운트 (마지막 관리자 보호)
                · updateMemberRole     : 회원 role 변경 (정지/활성화/승격/강등 공용)
                · role 값 정의: USER / USER_BANNED / ADMIN
============================================================
-->
<mapper namespace="admin">

	<!-- =====================================================================
		1. 대시보드 - 회원/공연/예매 카운트 한 번에
	===================================================================== -->
	<select id="selectDashboard" resultType="hashMap">
		SELECT
			(SELECT COUNT(*) FROM members)  AS MEMBER_CNT,
			(SELECT COUNT(*) FROM concerts) AS CONCERT_CNT,
			(SELECT COUNT(*) FROM bookings) AS BOOKING_CNT
		FROM dual
	</select>


	<!-- =====================================================================
		2. 공연 - 목록 (검색/페이징)
	===================================================================== -->
	<select id="selectConcertList" parameterType="hashMap" resultType="hashMap">
		SELECT *
		FROM (
			SELECT
				ROWNUM AS RNUM,
				A.*
			FROM (
				SELECT
					c.concert_id,
					c.title,
					c.artist,
					c.venue,
					c.status,
					c.thumbnail,
					COUNT(*) OVER() AS TOTAL_COUNT
				FROM   concerts c
				<where>
					<if test="status != null and status != ''">
						AND c.status = #{status}
					</if>
					<if test="keyword != null and keyword != ''">
						<choose>
							<when test="searchType == 'title'">
								AND LOWER(c.title)  LIKE '%' || LOWER(#{keyword}) || '%'
							</when>
							<when test="searchType == 'artist'">
								AND LOWER(c.artist) LIKE '%' || LOWER(#{keyword}) || '%'
							</when>
							<otherwise>
								AND (
									LOWER(c.title)  LIKE '%' || LOWER(#{keyword}) || '%'
									OR LOWER(c.artist) LIKE '%' || LOWER(#{keyword}) || '%'
								)
							</otherwise>
						</choose>
					</if>
				</where>
				ORDER BY c.concert_id DESC
			) A
			WHERE ROWNUM &lt;= #{END}
		)
		WHERE RNUM &gt;= #{START}
	</select>


	<!-- =====================================================================
		3. 공연 - 단건 조회
	===================================================================== -->
	<select id="selectConcert" parameterType="hashMap" resultType="hashMap">
		SELECT
			concert_id,
			title,
			artist,
			venue,
			description,
			thumbnail,
			status
		FROM   concerts
		WHERE  concert_id = #{concertId}
	</select>


	<!-- =====================================================================
		4. 공연 - 등록
	===================================================================== -->
	<insert id="insertConcert" parameterType="hashMap">
		INSERT INTO concerts (
			title,
			artist,
			venue,
			description,
			thumbnail,
			status
		) VALUES (
			#{title},
			#{artist},
			#{venue},
			#{description, jdbcType=CLOB},
			#{thumbnail},
			<choose>
				<when test="status != null and status != ''">#{status}</when>
				<otherwise>'UPCOMING'</otherwise>
			</choose>
		)
	</insert>


	<!-- =====================================================================
		5. 공연 - 수정
	===================================================================== -->
	<update id="updateConcert" parameterType="hashMap">
		UPDATE concerts
		SET
			title       = #{title},
			artist      = #{artist},
			venue       = #{venue},
			description = #{description, jdbcType=CLOB},
			thumbnail   = #{thumbnail},
			status      = #{status}
		WHERE concert_id = #{concertId}
	</update>


	<!-- =====================================================================
		6-1. 공연 삭제 - 예매 건수 카운트 (스마트 삭제 판정용)
	===================================================================== -->
	<select id="countBookingsByConcert" parameterType="hashMap" resultType="int">
		SELECT COUNT(*)
		FROM   bookings           b
		JOIN   concert_schedules  s ON b.schedule_id = s.schedule_id
		WHERE  s.concert_id = #{concertId}
	</select>


	<!-- =====================================================================
		6-2. 공연 삭제 - Soft : status = 'CLOSED' 만 변경
	===================================================================== -->
	<update id="deleteConcert" parameterType="hashMap">
		UPDATE concerts
		SET    status = 'CLOSED'
		WHERE  concert_id = #{concertId}
	</update>


	<!-- =====================================================================
		6-3. 공연 삭제 - Hard : 공연 row 자체 삭제
	===================================================================== -->
	<delete id="deleteConcertHard" parameterType="hashMap">
		DELETE FROM concerts
		WHERE  concert_id = #{concertId}
	</delete>


	<!-- =====================================================================
		6-4. 공연 산하 데이터 삭제 - FK 역순
	===================================================================== -->
	<delete id="deleteBookingItemsByConcert" parameterType="hashMap">
		DELETE FROM booking_items
		WHERE  booking_id IN (
			SELECT b.booking_id
			FROM   bookings           b
			JOIN   concert_schedules  s ON b.schedule_id = s.schedule_id
			WHERE  s.concert_id = #{concertId}
		)
	</delete>

	<delete id="deletePaymentsByConcert" parameterType="hashMap">
		DELETE FROM payments
		WHERE  booking_id IN (
			SELECT b.booking_id
			FROM   bookings           b
			JOIN   concert_schedules  s ON b.schedule_id = s.schedule_id
			WHERE  s.concert_id = #{concertId}
		)
	</delete>

	<delete id="deleteBookingsByConcert" parameterType="hashMap">
		DELETE FROM bookings
		WHERE  schedule_id IN (
			SELECT schedule_id
			FROM   concert_schedules
			WHERE  concert_id = #{concertId}
		)
	</delete>

	<delete id="deleteSeatsByConcert" parameterType="hashMap">
		DELETE FROM seats
		WHERE  schedule_id IN (
			SELECT schedule_id
			FROM   concert_schedules
			WHERE  concert_id = #{concertId}
		)
	</delete>

	<delete id="deleteSchedulesByConcert" parameterType="hashMap">
		DELETE FROM concert_schedules
		WHERE  concert_id = #{concertId}
	</delete>


	<!-- =====================================================================
		7. 회원 - 목록 (검색/페이징)
		     ▶ role 필터는 USER / USER_BANNED / ADMIN 3가지 + 빈값
	===================================================================== -->
	<select id="selectMemberList" parameterType="hashMap" resultType="hashMap">
		SELECT *
		FROM (
			SELECT
				ROWNUM AS RNUM,
				A.*
			FROM (
				SELECT
					m.member_id,
					m.email,
					m.name,
					m.phone,
					m.birth_date,
					m.role,
					COUNT(*) OVER() AS TOTAL_COUNT
				FROM   members m
				<where>
					<if test="role != null and role != ''">
						AND m.role = #{role}
					</if>
					<if test="keyword != null and keyword != ''">
						<choose>
							<when test="searchType == 'email'">
								AND LOWER(m.email) LIKE '%' || LOWER(#{keyword}) || '%'
							</when>
							<when test="searchType == 'name'">
								AND LOWER(m.name)  LIKE '%' || LOWER(#{keyword}) || '%'
							</when>
							<otherwise>
								AND (
									LOWER(m.email) LIKE '%' || LOWER(#{keyword}) || '%'
									OR LOWER(m.name)  LIKE '%' || LOWER(#{keyword}) || '%'
								)
							</otherwise>
						</choose>
					</if>
				</where>
				ORDER BY m.member_id DESC
			) A
			WHERE ROWNUM &lt;= #{END}
		)
		WHERE RNUM &gt;= #{START}
	</select>


	<!-- =====================================================================
		7-1. 회원 - 단건 조회 (권한 변경 전 현재 role 확인용)
	===================================================================== -->
	<select id="selectMember" parameterType="hashMap" resultType="hashMap">
		SELECT
			member_id,
			email,
			name,
			role
		FROM   members
		WHERE  member_id = #{memberId}
	</select>


	<!-- =====================================================================
		7-2. 관리자(ADMIN) 카운트 - 마지막 관리자 보호용
		      ▶ 강등 시 ADMIN 카운트가 1 이하면 차단
	===================================================================== -->
	<select id="countAdmin" resultType="int">
		SELECT COUNT(*)
		FROM   members
		WHERE  role = 'ADMIN'
	</select>


	<!-- =====================================================================
		7-3. 회원 role 변경 (정지/활성화/승격/강등 공용)
		      ▶ 호출하는 Service 메서드에 따라 newRole 만 다르게 전달
		      ▶ USER         → 일반 회원 (활성)
		      ▶ USER_BANNED  → 일반 회원 (정지)
		      ▶ ADMIN        → 관리자
	===================================================================== -->
	<update id="updateMemberRole" parameterType="hashMap">
		UPDATE members
		SET    role = #{newRole}
		WHERE  member_id = #{memberId}
	</update>


	<!-- =====================================================================
		8. 예매 - 목록 (검색/페이징)
	===================================================================== -->
	<select id="selectBookingList" parameterType="hashMap" resultType="hashMap">
		SELECT *
		FROM (
			SELECT
				ROWNUM AS RNUM,
				A.*
			FROM (
				SELECT
					b.booking_id,
					b.total_price,
					b.status            AS booking_status,
					TO_CHAR(b.created_at, 'YYYY-MM-DD HH24:MI:SS') AS created_at,
					m.member_id,
					m.email,
					m.name              AS member_name,
					c.concert_id,
					c.title             AS concert_title,
					c.artist,
					TO_CHAR(s.performance_date, 'YYYY-MM-DD HH24:MI') AS performance_date,
					COUNT(*) OVER()     AS TOTAL_COUNT
				FROM        bookings           b
				INNER JOIN  members            m ON b.member_id   = m.member_id
				INNER JOIN  concert_schedules  s ON b.schedule_id = s.schedule_id
				INNER JOIN  concerts           c ON s.concert_id  = c.concert_id
				<where>
					<if test="status != null and status != ''">
						AND b.status = #{status}
					</if>
					<if test="keyword != null and keyword != ''">
						<choose>
							<when test="searchType == 'email'">
								AND LOWER(m.email) LIKE '%' || LOWER(#{keyword}) || '%'
							</when>
							<when test="searchType == 'name'">
								AND LOWER(m.name)  LIKE '%' || LOWER(#{keyword}) || '%'
							</when>
							<when test="searchType == 'concert'">
								AND LOWER(c.title) LIKE '%' || LOWER(#{keyword}) || '%'
							</when>
							<otherwise>
								AND (
									LOWER(m.email) LIKE '%' || LOWER(#{keyword}) || '%'
									OR LOWER(m.name)  LIKE '%' || LOWER(#{keyword}) || '%'
									OR LOWER(c.title) LIKE '%' || LOWER(#{keyword}) || '%'
								)
							</otherwise>
						</choose>
					</if>
				</where>
				ORDER BY b.booking_id DESC
			) A
			WHERE ROWNUM &lt;= #{END}
		)
		WHERE RNUM &gt;= #{START}
	</select>

</mapper>