<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
============================================================
 Project   : 관제 티켓 (Ticketing System)
 FileName  : Admin_SQL.xml
 Namespace : admin

 Modified  : 2026.05.25

 Description :
   - 관리자 화면 SQL (MyBatis)
   - 실제 테이블: members / concerts / concert_schedules / bookings

 History :
   2026.05.25 - 탬퍼 감지 SQL 제거 (보안 모니터링은 Splunk 측에서 처리)
                payments 테이블 JOIN 제거
============================================================
-->
<mapper namespace="admin">

	<!-- =====================================================================
		1. 대시보드 - 회원/공연/예매 카운트 한 번에
	===================================================================== -->
	<select id="selectDashboard" resultType="hashMap">
		SELECT
			(SELECT COUNT(*) FROM members)  AS MEMBER_CNT,
			(SELECT COUNT(*) FROM concerts) AS CONCERT_CNT,
			(SELECT COUNT(*) FROM bookings) AS BOOKING_CNT
		FROM dual
	</select>


	<!-- =====================================================================
		2. 공연 - 목록 (검색/페이징)
		     ▶ START / END : AbstractDao.selectPagingList 에서 자동 세팅
		     ▶ TOTAL_COUNT : 검색 조건 적용된 전체 건수
		     ▶ 검색 컬럼   : title / artist  (searchType)
	===================================================================== -->
	<select id="selectConcertList" parameterType="hashMap" resultType="hashMap">
		SELECT *
		FROM (
			SELECT
				ROWNUM AS RNUM,
				A.*
			FROM (
				SELECT
					c.concert_id,
					c.title,
					c.artist,
					c.venue,
					c.status,
					c.thumbnail,
					COUNT(*) OVER() AS TOTAL_COUNT
				FROM   concerts c
				<where>
					<if test="status != null and status != ''">
						AND c.status = #{status}
					</if>
					<if test="keyword != null and keyword != ''">
						<choose>
							<when test="searchType == 'title'">
								AND c.title  LIKE '%' || #{keyword} || '%'
							</when>
							<when test="searchType == 'artist'">
								AND c.artist LIKE '%' || #{keyword} || '%'
							</when>
							<otherwise>
								AND (
									c.title  LIKE '%' || #{keyword} || '%'
									OR c.artist LIKE '%' || #{keyword} || '%'
								)
							</otherwise>
						</choose>
					</if>
				</where>
				ORDER BY c.concert_id DESC
			) A
			WHERE ROWNUM &lt;= #{END}
		)
		WHERE RNUM &gt;= #{START}
	</select>


	<!-- =====================================================================
		3. 공연 - 단건 조회
	===================================================================== -->
	<select id="selectConcert" parameterType="hashMap" resultType="hashMap">
		SELECT
			concert_id,
			title,
			artist,
			venue,
			description,
			thumbnail,
			status
		FROM   concerts
		WHERE  concert_id = #{concertId}
	</select>


	<!-- =====================================================================
		4. 공연 - 등록
		     ▶ concert_id 는 IDENTITY (자동 생성)
		     ▶ status 미지정 시 DEFAULT 'UPCOMING'
	===================================================================== -->
	<insert id="insertConcert" parameterType="hashMap">
		INSERT INTO concerts (
			title,
			artist,
			venue,
			description,
			thumbnail,
			status
		) VALUES (
			#{title},
			#{artist},
			#{venue},
			#{description, jdbcType=CLOB},
			#{thumbnail},
			<choose>
				<when test="status != null and status != ''">#{status}</when>
				<otherwise>'UPCOMING'</otherwise>
			</choose>
		)
	</insert>


	<!-- =====================================================================
		5. 공연 - 수정
	===================================================================== -->
	<update id="updateConcert" parameterType="hashMap">
		UPDATE concerts
		SET
			title       = #{title},
			artist      = #{artist},
			venue       = #{venue},
			description = #{description, jdbcType=CLOB},
			thumbnail   = #{thumbnail},
			status      = #{status}
		WHERE concert_id = #{concertId}
	</update>


	<!-- =====================================================================
		6. 공연 - 삭제 (soft delete : status='CLOSED')
		     ▶ 실제 row 는 남기고 상태만 변경 (FK 무결성, 감사 추적 용도)
	===================================================================== -->
	<update id="deleteConcert" parameterType="hashMap">
		UPDATE concerts
		SET    status = 'CLOSED'
		WHERE  concert_id = #{concertId}
	</update>


	<!-- =====================================================================
		7. 회원 - 목록 (검색/페이징)
		     ▶ 검색 컬럼 : email / name  (searchType)
	===================================================================== -->
	<select id="selectMemberList" parameterType="hashMap" resultType="hashMap">
		SELECT *
		FROM (
			SELECT
				ROWNUM AS RNUM,
				A.*
			FROM (
				SELECT
					m.member_id,
					m.email,
					m.name,
					m.phone,
					m.birth_date,
					m.role,
					COUNT(*) OVER() AS TOTAL_COUNT
				FROM   members m
				<where>
					<if test="role != null and role != ''">
						AND m.role = #{role}
					</if>
					<if test="keyword != null and keyword != ''">
						<choose>
							<when test="searchType == 'email'">
								AND m.email LIKE '%' || #{keyword} || '%'
							</when>
							<when test="searchType == 'name'">
								AND m.name  LIKE '%' || #{keyword} || '%'
							</when>
							<otherwise>
								AND (
									m.email LIKE '%' || #{keyword} || '%'
									OR m.name  LIKE '%' || #{keyword} || '%'
								)
							</otherwise>
						</choose>
					</if>
				</where>
				ORDER BY m.member_id DESC
			) A
			WHERE ROWNUM &lt;= #{END}
		)
		WHERE RNUM &gt;= #{START}
	</select>


	<!-- =====================================================================
		8. 예매 - 목록 (검색/페이징)
		     ▶ 회원/공연/일정 JOIN
		     ▶ 검색 컬럼 : email / name / concert  (searchType)
		     ▶ 상태 필터 : status (PENDING/CONFIRMED/CANCELLED)
	===================================================================== -->
	<select id="selectBookingList" parameterType="hashMap" resultType="hashMap">
		SELECT *
		FROM (
			SELECT
				ROWNUM AS RNUM,
				A.*
			FROM (
				SELECT
					b.booking_id,
					b.total_price,
					b.status            AS booking_status,
					TO_CHAR(b.created_at, 'YYYY-MM-DD HH24:MI:SS') AS created_at,
					m.member_id,
					m.email,
					m.name              AS member_name,
					c.concert_id,
					c.title             AS concert_title,
					c.artist,
					TO_CHAR(s.performance_date, 'YYYY-MM-DD HH24:MI') AS performance_date,
					COUNT(*) OVER()     AS TOTAL_COUNT
				FROM        bookings           b
				INNER JOIN  members            m ON b.member_id   = m.member_id
				INNER JOIN  concert_schedules  s ON b.schedule_id = s.schedule_id
				INNER JOIN  concerts           c ON s.concert_id  = c.concert_id
				<where>
					<if test="status != null and status != ''">
						AND b.status = #{status}
					</if>
					<if test="keyword != null and keyword != ''">
						<choose>
							<when test="searchType == 'email'">
								AND m.email LIKE '%' || #{keyword} || '%'
							</when>
							<when test="searchType == 'name'">
								AND m.name  LIKE '%' || #{keyword} || '%'
							</when>
							<when test="searchType == 'concert'">
								AND c.title LIKE '%' || #{keyword} || '%'
							</when>
							<otherwise>
								AND (
									m.email LIKE '%' || #{keyword} || '%'
									OR m.name  LIKE '%' || #{keyword} || '%'
									OR c.title LIKE '%' || #{keyword} || '%'
								)
							</otherwise>
						</choose>
					</if>
				</where>
				ORDER BY b.booking_id DESC
			) A
			WHERE ROWNUM &lt;= #{END}
		)
		WHERE RNUM &gt;= #{START}
	</select>

</mapper>