<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="payment">

    <!-- 1. Map에 담긴 Key 이름 기반으로 인서트 -->
    <insert id="insertPayment" parameterType="java.util.HashMap">
        INSERT INTO payments (
            booking_id,
            user_id,
            transaction_id,
            requested_amount,
            status
        ) VALUES (
            #{booking_id},
            #{user_id},
            #{transaction_id},
            #{requested_amount},
            #{status}
        )
    </insert>

    <!-- 2. 영수증 출력을 위한 단건 상세 조회 (결과를 Map 형태로 반환) -->
    <select id="selectPaymentById" parameterType="String" resultType="java.util.HashMap">
        SELECT 
            payment_id,
            booking_id,
            user_id,
            transaction_id,
            requested_amount,
            status
        FROM 
            payments
        WHERE 
            transaction_id = #{transactionId}
    </select>

    <!-- 3. 실시간 트래픽 관제용 전체 리스트 조회 (결과 리스트를 Map 형태로 반환) -->
    <select id="selectPaymentList" resultType="java.util.HashMap">
        SELECT 
            payment_id,
            booking_id,
            user_id,
            transaction_id,
            requested_amount,
            status
        FROM 
            payments
        ORDER BY 
            payment_id DESC
    </select>

</mapper>